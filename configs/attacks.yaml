data_dir: "data"
payload_dir: "assets/attack_payloads"

budgets:
  B_tokens: [50, 150, 300, 600]
  K_sources: [1, 2, 4]

channels: ["retrieval", "tool_output"]
strategies: ["non_adaptive", "goal_laundering", "evidence_laundering", "policy_mimicry"]

injection:
  placement: ["prefix", "suffix", "middle"]
  max_payloads_per_episode: 1
